import PropTypes from 'prop-types';

const NoChannel = (props) => {
  const {} = props;
  return (
    <div>
      no channel
    </div>
  );
};

NoChannel.propTypes = {

};

export default NoChannel;
