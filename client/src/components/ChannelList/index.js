import React from 'react';
import PropTypes from 'prop-types';

const ChannelList = (props) => {
  const {} = props;
  return (
    <div>
      channels of openend server
    </div>
  );
};

ChannelList.propTypes = {

};

export default ChannelList;
